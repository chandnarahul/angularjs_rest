var restApiDemo = angular.module('currentView');

restApiDemo.component('currentView', {
    templateUrl: 'templates/default_view.html',
    controller:
        function postDataController($scope, $http) {
        }
});